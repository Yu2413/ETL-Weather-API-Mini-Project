# ETL-Weather-API-Mini-Project
This mini-project demonstrates how to build a clean ETL (Extract–Transform–Load) workflow in Python using a real, public API. The script retrieves current weather data for Columbus, Ohio, parses the JSON response, and prepares it for downstream analytics using pandas or database loading with SQLAlchemy.
